$("#tableHolder").html('<%= escape_javascript(render 'table') %>')
$('.deleteButton').button({
            icons: {
                primary: "ui-icon-trash"
            },
            text: false
        });
$("#show_days").html("Days Remaining:<%=@days_remaining%>");
