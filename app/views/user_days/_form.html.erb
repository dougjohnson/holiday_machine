<%= simple_form_for(@user_day) do |f| %>
  <% if @user_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_day.errors.count, "error") %> prohibited this user_day from being saved:</h2>

      <ul>
      <% @user_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
